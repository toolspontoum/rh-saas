{
  "name": "vv-consulting",
  "private": true,
  "description": "Raiz do monorepo: script único para subir API + Web em desenvolvimento.",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
    "build:web": "npm run build --workspace=vv-consulting-web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
